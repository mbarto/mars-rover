import React, {Component} from 'react';

export default class Help extends Component {
    render() {
        return (<div className="help">
        Click on cells to create / remove obstacles.<br/>
        Use buttons to rotate / move the rover.<br/>
        </div>);
    }
}